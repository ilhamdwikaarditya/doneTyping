You can use function doneTyping for barcode system (scan -> value on field -> auto process use doneTyping ) or etc. 
<br />
<br />
You can call the method with: <br />

$("#your-id").donetyping( function () { 
	console.log('Hello bos'); 
});